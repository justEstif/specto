---
# project-media-consumption-analysis-86sq
title: Build sharing page end-to-end
status: completed
type: feature
priority: normal
created_at: 2026-03-13T21:20:34Z
updated_at: 2026-03-14T15:17:38Z
---

## Todo

- [x] Create `share_profiles` table migration (010_create_share_profiles) (blocks JSONB, excluded_platforms TEXT[], excluded_tags TEXT[])
- [x] Add `private BOOLEAN DEFAULT false` column to `media_items` (011_add_media_item_private)
- [x] Write sqlc queries for share profile CRUD + block data aggregation
- [x] Create `ShareProfileStore` interface in core/stores.go\n- [x] Create pgx store implementation (share_profile_store.go)
- [x] Implement API endpoints: GET/PUT `/api/v1/share-profile`, GET `/api/v1/share-profile/preview`, POST `/api/v1/items/{id}/privacy`
- [x] Wire real data into `ShareProfilePage` handler (replace hardcoded empty blocks)
- [ ] Upgrade excluded platforms input to searchable multi-select component
- [ ] Upgrade excluded tags input to searchable multi-select component
- [ ] Add per-block platform filter multi-selects (in wireframe but missing from template)
- [x] Wire Save & publish button to API
- [ ] Wire Sortable.js for block drag reorder
- [x] Register share-profile API routes in main.go

## Summary of Changes

### Files Created
- `migrations/010_create_share_profiles.up.sql` / `.down.sql` — share_profiles table
- `migrations/011_add_media_item_private.up.sql` / `.down.sql` — private column on media_items
- `internal/core/store/share_profile_store.go` — PgShareProfileStore implementation
- `internal/handlers/share.go` — Share profile API handlers (GET/PUT share-profile, preview, item privacy)

### Files Modified
- `internal/database/queries.sql` — Added 8 new queries (GetShareProfile, GetShareProfileBySlug, UpsertShareProfile, SetItemPrivacy, GetPublicItems, GetPublicTagDistribution, GetPublicTopCreators, GetPublicPlatformMix)
- `internal/database/queries.sql.go` — Regenerated by sqlc
- `internal/database/models.go` — Regenerated by sqlc (new ShareProfile model, private field on MediaItem)
- `internal/core/stores.go` — Added ShareProfile, ShareBlock, PublicShareProfile, TopCreatorEntry, PlatformMixEntry types + ShareProfileStore interface
- `internal/core/store/queries.go` — Added share profile methods to Querier interface
- `internal/app/app.go` — Wire ShareProfileStore into App, fix pre-existing NewSyncService arg mismatch
- `internal/handlers/share_page.go` — Replaced placeholder blocks with real data from ShareProfileStore
- `internal/handlers/settings_page.go` — Load ShareProfile for sharing tab
- `components/settings.templ` — Added ShareProfile field to SettingsData, wired Save & publish button to API, dynamic blocks from profile data
- `components/settings_templ.go` — Regenerated by templ
- `cmd/web/main.go` — Register share-profile API routes and item privacy route

### Deferred UI Enhancements
The following UI polish items remain for future work:
- Searchable multi-select for excluded platforms/tags (currently comma-separated text input)
- Per-block platform filter dropdowns
- Sortable.js for block drag reorder

## Summary of Changes\n\nImplemented sharing page end-to-end:\n- Created `share_profiles` table migration (JSONB blocks, TEXT[] exclusions, slug, published)\n- Added `private` column to `media_items` with partial index\n- Added 8 SQL queries for share profile CRUD + public data aggregation\n- Created `ShareProfile`, `ShareBlock`, `PublicShareProfile` domain types + `ShareProfileStore` interface\n- Implemented `PgShareProfileStore` in `internal/core/store/share_profile_store.go`\n- Created API handlers in `internal/handlers/share.go` (GET/PUT share-profile, preview, item privacy)\n- Wired real data into existing share profile and settings templates\n- Registered all routes in `main.go`\n- Build and all tests pass\n\nDeferred: multi-select components, Sortable.js drag reorder, per-block platform filters (UI polish).
